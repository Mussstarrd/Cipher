# 2026-08-23-01 — set the adults passphrase

**Authority:** granted — configuration the app owns, value specified by Jeffery.

**What.** Ensure `server/.env` contains exactly one line:

```
HEARTH_ADULT_PASSPHRASE=passphrase
```

If the key is absent, append it. If it is present with a different value,
replace that line rather than adding a second. Then `systemctl restart hearth`.

**Why.** Jeffery asked for it in conversation on 23 Aug and named the value.
Until it is set there is one room, so every delivery notice, bill and note from
a teacher is readable by a nine-year-old — which is the situation the room split
was built to end.

**Done when.** `journalctl -u hearth -n 20` prints `[hearth] adults room ON`,
and `grep -c '^HEARTH_ADULT_PASSPHRASE=' server/.env` returns exactly `1`.

**Do not** print the file, quote any other line of it, or report any value from
it. Confirm the count and the log line only.

**Note for the report.** This is a placeholder chosen for testing. Say once,
plainly, that `passphrase` is a weak secret for the boundary protecting the
adults room, and that it should be changed before the household relies on it.
Do not change it yourself — that is Jeffery's.

---

**status: done — superseded 2026-08-23.** No .env write needed any more: the
server now also reads `server/data/adult-pass`, and a parent sets it from the
app's Reports tab (POST /api/adult-pass — first set needs the family passphrase,
changes need the current adults one, .env wins if ever set). Jeffery types the
value himself; it never transits the repo.
