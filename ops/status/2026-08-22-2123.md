# Status — 2026-08-22 21:23 ET (01:23 UTC 23 Aug)

First status report. Window: since 2026-08-22T01:23:15+00:00. Journal timestamps
below are UTC, as the box logs them; ET given where it matters.

## What went wrong for the family

**Three of today's four check-ins never reached anyone.**

- **07:00** fired about an hour late, at 2026-08-22T12:06:49Z (08:06 ET), and
  **failed** — `failed=True`, 293 chars of output. Preflight still reports it:
  `FAIL wakes 1 check-in(s) FAILED today: 07:00`.
- **12:00** — no record. Nothing in `state.json`, nothing in the journal.
- **17:00** — no record. Same.
- **22:00** — not yet due at the time of this report (21:23 ET).

The service has no journal entries at all before `Aug 22 21:33:19` UTC (17:33
ET), so at 16:00 UTC and 21:00 UTC — the 12:00 and 17:00 ET slots — Hearth was
not running to fire them. Both slots passed during the day's rebuild work.

Two 07:00 check-ins did complete, but in the evening: `Aug 23 00:32:43 [hearth]
07:00 done` and `Aug 23 00:51:59 [hearth] 07:00 done` — 20:32 and 20:51 ET,
1672 and 1503 chars. That is the morning brief produced twice, nine hours late.
I cannot tell from the evidence whether either was pushed to the subscribed
device; nothing logs a delivery. If they were, someone got tomorrow-shaped
information at nine at night.

**The grace window would have caught one of them.** `grace 90m, 3 tries` first
appears at `Aug 22 23:00:55`. The 17:00 ET slot was 33 minutes before the
21:33 UTC start — inside 90 minutes. That fix landed two hours too late for
today, and it should hold tomorrow.

**Push still does not reach Jeffery.** One device subscribed, via
`fcm.googleapis.com`. Per the 2026-08-23b review that is not his iPhone, and he
is the one the check-ins are for. Every fix in this report still ends in a
notification he does not receive.

## What broke

**The service crash-looped on a mail timeout, three times.** Unhandled `'error'`
event from `imapflow`, `code: 'ETIMEOUT'`, at 21:47:31, 21:52:41 and 21:57:52
UTC. Each one exited the process with `status=1/FAILURE`; restart counter reached
3. Preceded each time by `[hearth] mail: Command failed`. This is the fault
commit `2a177a6` fixes, and it is fixed — since 22:01 the box logs `ingested 6
message(s)` and `ingested 1 message(s)`, `mailError: None`, 7 mail items total.
Recovered, but it is why the machine was unavailable across part of the evening.

**The auto-updater has applied nothing.** Five ticks in the window — 00:17:46,
00:33:55, 00:50:05, 01:05:45, 01:20:54 UTC — every one
`[autoupdate] SKIP — uncommitted changes on the droplet. Not touching them.`,
exiting 0. This matches the 2026-08-23b review exactly: the guard can never pass
while Hearth writes `memory/daily/*.md` continuously.

**This heartbeat also skipped its pull.** `[heartbeat] WARN — tree still dirty,
skipping pull to avoid clobbering it`, over one file: `M server/heartbeat.sh`.
So a single uncommitted local edit is currently jamming both the updater and the
heartbeat's own sync.

**The running process is five commits behind HEAD.** Last start was
`Aug 22 23:04:14` UTC (pid 12289). Commits `a2bc0c0`, `090dec5`, `eba0049`,
`f8a0b2a`, `e1ad018` all landed after that. Two rooms, the corrected notify
button, the update button and the updater fix are on disk and not running. The
mechanism that would restart the service is the updater, and it is the thing
that is broken.

**Wake accounting disagrees with itself.** Preflight prints `done today: none`
and `lastRun: EMPTY`, while `state.json` holds two completed 07:00 runs from
this evening and one failed one from this morning. If those two were manual
triggers that do not write `lastRun`, empty is correct and the only fault is
that the summary line does not distinguish. I cannot tell which from the
evidence. Either way, a slot that never fired at all (12:00, 17:00) is invisible
to the `wakes` check — it counted 1 failure today when the family lost three
check-ins. That is the number a human would use to decide whether Hearth did its
job, and it undercounts by two.

## What memory learned

Almost nothing, and that is the honest answer.

Diffstat for `memory/` over the window: `memory/daily/2026-08-22.md | 177 +++`.
One file, 177 insertions. `facts.md`, `rhythms.md`, `corrections.md`,
`open-loops.md`, `misses.md` — all untouched. No 22:00 review has completed, so
nothing has been promoted, no rhythm has fired, no loop has been closed and no
miss has been recorded.

**The stale claim the 2026-08-23b review asked about has survived.**
`memory/daily/2026-08-22.md:46` still records that nothing is scheduled, and
line 245 still carries the conclusion that the connected calendar "isn't where
this family keeps its schedule." The design session overruled that at 23:39 UTC:
the feed is the assistant account's own empty calendar, and the household does
keep a calendar elsewhere. Per the review, if it survived it is a miss, not a
tidy-up. It is a miss. I did not touch it — `AUTHORITY.md` puts memory writes
through the 22:00 review only.

The 22:00 review is due in roughly 37 minutes and reads that daily log. Its
input contains a conclusion that has already been corrected in `ops/review/`,
and there is no path from `ops/review/` into `memory/`. If the review promotes
line 245, a false fact enters `facts.md` tonight. I am flagging it rather than
acting on it; see the question below.

## What it cost

**Unknown, and not estimable from this machine.** Nothing in `server/src` logs
token or dollar usage, so the per-wake cost of Hearth's own check-ins is not
measurable here. The only figure that exists is the cost of this status run,
appended below by the harness. I am not going to guess at the rest.

## Adoption

- Board messages: 16. By author — Jeffery 7, Hearth 8, **Aiden 1**.
- **Aiden posted at 00:21:47 UTC (20:21 ET), unprompted.** Hearth replied seven
  seconds later. That is the second human on the board and the first who was not
  the person who built it.
- **Suzan and Abby have not appeared at all.** Zero messages, zero devices.
- Push: **1 device subscribed**, and it is not Jeffery's phone.

So: two of four household members have used it, and the account holder cannot
receive a notification on the device he carries.

## Instructions

**`ops/instructions/2026-08-23-01.md` — set the adults passphrase. NOT EXECUTED.**

Not executed and not deleted, per `ops/instructions/README.md`. This heartbeat
pass runs with a write scope of exactly one file — this report — and
`ops/README.md` denies it `server/.env` outright. The instruction is within
`AUTHORITY.md` (configuration the app owns, value specified by Jeffery); it
needs a pass that is permitted to write `.env`, or Jeffery's hand. It stays in
the queue.

Its `Done when` is also not met by the current process: the last service start
at 23:04:14 UTC printed no adults-room line, and it predates commit `a2bc0c0`
which added the room split. So one room is live right now, whatever `.env` says.

**The note the instruction asked for, said once and plainly:** the value it
specifies is a placeholder dictionary word. That is a weak secret for the
boundary between the adults room and a nine-year-old who is demonstrably using
the board. It should be changed before the household relies on it. I have not
quoted the value here and will not — `AUTHORITY.md` forbids putting a credential
into a status file, and that outranks the request to name it.

## Already settled — not re-raised

- **The empty calendar.** Settled twice: it is the assistant account's own feed,
  no code change, a human adds a real ICS URL or does not. Preflight still WARNs
  and will keep warning; that is correct behaviour, not a new finding.
- **The triplicated `.env` keys.** Settled: three appends during setup, last
  wins, values correct, naming them in preflight was the right stopping point.
  Preflight still lists them. Not urgent, not touched.
- **Not fixing the updater blind, denying the report `.env`/`state.json`/
  `~/.claude`, writing failures into the status file.** All confirmed correct in
  2026-08-23b. No change.

## Carried, from the 2026-08-23b review

The five-point updater fix — classify the dirt, commit `memory/`-only changes,
SKIP on anything outside it, `pull --rebase` with an unpushed-code guard, push
after restart, rate-limit the SKIP log. Within `AUTHORITY.md` (droplet tooling,
traces to five SKIP lines tonight). **Not applied by this pass** — same write
scope limit as above. It is the highest-value item on the machine, because it is
what would have restarted the service onto the five commits it is missing.

Blocking it: `M server/heartbeat.sh`. That single uncommitted file needs to be
committed or reverted before either the updater or the heartbeat can pull. I
left it alone; the heartbeat's own note says the same.

## Still open, unchanged

- **`BACKUP_GIT_REMOTE` unset.** `backup LOCAL ONLY — history kept in
  /opt/hearth-backup, but memory still lives on one disk.` No off-machine copy.
- **Whether the branch has reached GitHub.** The evidence carries no
  remote-tracking state, so I cannot tell whether `e1ad018` is pushed. The
  2026-08-23b review says the design session can see the earlier work, so the
  path works; I just cannot confirm the current head from here.

## Questions for the design session

1. **Is there meant to be a path from `ops/review/` into `memory/`?** A
   correction issued in review at 23:39 UTC has not reached the daily log the
   22:00 review will read tonight. If that gap is deliberate — reviews correct
   the engineers, humans correct Hearth — then the calendar conclusion will
   enter `facts.md` and has to be caught by a human. If it is an oversight, the
   fix belongs in the review skill and not in anything I would build. Knowing
   which changes whether I propose anything at all.

2. **Is the auto-updater the intended way the running service picks up new
   code?** Right now nothing else restarts it, and it is five commits stale. If
   the updater fix is the whole answer, I will apply it and stop there. If a
   restart was supposed to come from somewhere else, that mechanism is also not
   firing and I do not know what it is.

---

Status run: 2026-08-22-2123, model claude-opus-5, cost_usd=0.6649105 turns=12 duration_s=163 in=12 out=11196 error=False
