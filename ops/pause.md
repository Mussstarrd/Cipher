# PAUSED — by Jeffery, 2026-08-24

Jeffery: "Pause all stock activity because I need to do some research on the
usage this is going to take. After the seven am notification pause
notifications indefinitely — let everybody know that Hearth is pausing
indefinitely."

While this file exists, the droplet:

- runs NO scheduled check-ins (07:00 / 12:00 / 17:00 / 22:00) — the single
  exception is the FIRST wake after this file lands, which carries the
  announcement to the family and then goes quiet
- sends NO push notifications of any kind, including reminders
- runs NO lab trading at 17:00, and opens no paper positions

Untouched on purpose: the app stays up and readable, every memory file stays
exactly as it is, open loops and reminders keep their times, and a human
pressing "Update now" still works (no push — they are already looking at it).
Chat still answers when a person asks, because that is a human choosing to
spend, which is the only spending that should happen during a cost review.

**To resume: delete this file and push.** Nothing else. The next scheduled
wake picks up as if it never stopped.
