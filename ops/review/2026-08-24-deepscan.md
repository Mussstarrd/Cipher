# Deep scan — 2026-08-24 (run 2)

Rotating subsystem: brain.js. Evidence: droplet commits through 07:50Z, Sunday's
status blocks, the daily log's Evening review block. No scratchpad content read.

## 1 · Promises kept

- **4/4 wakes fired Sunday** — first clean full day, on the new accounting line.
- **First Sunday review + compaction RAN and compacted**: long-lived layers now
  ~5KB each (down from ~8–9KB); the review's self-summary is genuinely good —
  it closed Tuesday's early-dismissal problem with Suzan's newly-learned work
  schedule, root-caused the push failures correctly, and logged its own worst
  error unprompted (asserted an oven model "certain" from a feature list — the
  exact confident-invention failure the charter bans; it caught itself). B17:
  compaction verified. (One caveat below.)
- Restart-skip verified live: "no server changes — restart skipped" in journal.
- Adoption turned: ~40 channel messages, Suzan actively confirming facts,
  Aiden on his own phone, push subs back to 2.

## 2 · What broke, root-caused

- **The review filed NO topics and drafted no meal plan** — returned topics:{}
  on its first live run; no refusal lines, so it simply skipped the instruction
  under a heavy day. FIXED in prompt: topics made explicitly non-optional with
  the Sunday failure named, and a missing week-plan must be drafted whatever
  day it is. Tomorrow's scan checks meals.md holds a real week.
- **Token accounting reports "no calls recorded yet"** despite a full Sunday of
  calls on post-deploy code. record() and the heartbeat display both verified
  working locally, so the droplet failure is currently INVISIBLE — the catch
  around record() swallowed it. FIXED: failures now log loudly; the next
  heartbeat's warning section will name the actual error.

## 3 · Family behaviour

The turn we were waiting for: Suzan is an active user (correcting and
confirming facts in chat), Aiden claimed his own phone. B18's caution stands
but the trajectory reversed. Still human-blocked: real calendars (J1),
TeamSnap (J2 — practice is TOMORROW), off-machine backup (B5).

## 4 · Code drift (brain.js read in full)

The prompt stack has grown five personas (wake, answer, readPaper, review,
trader) sharing context() — coherent, no contradictions found. Two nits, noted
not fixed: SLOT_JOB's 22:00 text and review()'s user prompt overlap slightly
(harmless duplication); CAN_AND_CANNOT is injected into answer() but not
readPaper() (photo replies can't promise wrongly — acceptable). The
review-repair path has still never fired in anger post-fix; leave watched.

## 5 · On course?

Yes — and Sunday's review block is the first hard evidence the core loop earns
its cost: it turned a chat aside (Suzan's work hours) into the resolution of a
flagged household problem (Tuesday pickup) with no human coordination. That is
the product working as designed.

## Watch list for tomorrow

- meals.md holds a drafted week after tonight's review; topics filed.
- The usage-record error named in the next heartbeat, then fixed properly.
- Lab day one: 08:45 research note fired; 17:00 trader went watch-only with
  baselines; graded note Tuesday.
- TeamSnap accepted before Tuesday practice (human).
