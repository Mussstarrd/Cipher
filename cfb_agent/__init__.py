"""Cipher CFB — a college football spread-betting research agent.

Pipeline: refresh data -> build power ratings -> price every game ->
compare to market -> emit a weekly card of plays with confidence tiers ->
log bets -> settle and track ROI + closing line value.
"""

__version__ = "0.1.0"
