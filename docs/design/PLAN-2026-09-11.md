# The plan, as of 2026-09-11

**Status:** live plan. Supersedes the schedule half of `ROADMAP-graphical-beta.md`.
**Answers:** the owner's question, *"OK so what is the plan now"*

## Where we actually are

The machine underneath is further along than the picture, and by a wide margin.

| Layer | State |
|---|---|
| Simulation | A thousand agents at 165 fps. Flow fields, breaches, turret line of sight, mobs that do not all run the same errand. Deterministic, state-hash guarded. |
| Match loop | Scan cycle, untimed opening, player-called last wave, pack-up window, truck extraction. |
| Progression | Gear with one comparable power budget, loot, wave-clear upgrade picks, permanent levels and a skill tree. All of it reaches the hero. |
| Interface | Build wheel, kit panel, skill screen, truck screen, upgrade offer. |
| Rendering | URP, comic-book cel shader with ink outlines and halftone, overcast winter lighting, 159 tests, a self-screenshotting smoke test. |
| **Art** | **Capsules and cubes.** |

That last row is the whole problem, and it is a supply problem with a known price.

## The one thing only the owner can do

**Buy the art packs, about $135.** Everything visual is blocked behind it and nothing else is.
`docs/decisions/ADR-007` has the shopping list and the licence verdicts.

Until that happens I build systems, not pictures. That is useful work and there is a lot of it left,
but it does not change what the game looks like, and looking at it is what the owner wants.

## What I do next, in order, without needing anything

### 1. Missions become data, not code

Right now there is one hard-coded arena. `docs/design/progression-and-campaign.md` §6 already specifies a
scenario JSON format and `campaign-act1.md` already lists twelve missions; none of it is loadable.

Building the loader turns the campaign from a document into content, and it is the prerequisite for every
mission after the first. It also lets the owner change a wave table without a rebuild.

### 2. The Gate, as an actual layout

Mission 1 built as real geometry: brick entry pillars, a guardhouse, a boom barrier, the wired-shut gate,
treeline on both flanks. Still untextured, but the *shape* of the level, which is what tells us whether the
mission plays before we spend a penny dressing it.

### 3. The interlude between positions

The scan cycle already computes leftover time and nothing consumes it. This is the phase where the owner's
"stealth up and trap up and fortify and rest" lives: spend the remaining hours on fortifying the next line,
laying traps, or resting. It closes the loop that extraction opens.

### 4. The things I owe from earlier

- Patrol drone waypoints. The simulation has been finished and tested for days; the game has no way to draw
  a route, so the feature is invisible.
- Cover props, so the open ground has texture as *cover* before it has texture as art.
- A post-processing pass. Bloom and tonemapping would push the comic look harder for roughly no cost.
- `docs/02-FLAGSHIP-PITCH.md` is still the old kingpin pitch and is now wrong in almost every particular.

### 5. Art pipeline, built before the art arrives

The crowd rendering technique in ADR-007 needs a bake tool that turns a rigged character into a mesh plus an
animation texture. That tool can be written and proven against a free placeholder character now, so the day
the packs land we are dressing the game rather than starting the pipeline.

## What changes the moment the purchase happens

| Day | Milestone |
|---|---|
| +0 | Packs in, environment kit imported |
| +5 | The Gate dressed: houses, trees, road, gate, props |
| +12 | Crowd bake pipeline live. **Real people walking at you.** |
| +18 | Hero model and animation, full dressing pass |
| +21 | **Playable graphical beta of Mission 1** |

Three weeks from the purchase, not from today. Every day the purchase waits, that date moves one day.

## What I am deliberately not doing

- **Android.** The variant explosion that blocked it is fixed, but validating on a device is a day of work
  that buys nothing until there is something worth looking at.
- **Couch co-op.** Phase 4. The loadout seam makes it cheap later; doing it now would double the surface of
  every system still in flux.
- **DOTS.** Rejected in ADR-007 with reasons. It costs the deterministic simulation, and that is not a trade
  worth making for batching we do not yet need.
- **More concept art.** We have enough to direct from. More pictures are not progress.
